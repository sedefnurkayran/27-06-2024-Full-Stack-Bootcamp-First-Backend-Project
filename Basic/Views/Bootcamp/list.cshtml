@model List<Bootcamp>

@* @section Header{
@await Html.PartialAsync("_Header")
} *@

@foreach (var bootcamp in Model)
{
   @if(bootcamp.isActive){
   @await Html.PartialAsync("_Bootcamp",bootcamp);
   }
}
